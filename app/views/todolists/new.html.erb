
<%# erbファイルはデータの入力のみを担当する ← この章だけ？ %>

<%# form_forは、インスタンス変数を取得し、ブロック変数「f」に渡している %>

<%# <% で囲まれている部分はフォームオブジェクト%>




<h1>新規投稿</h1>

<%= form_for(@list, url: '/todolists') do |f| %>

    <h4>タイトル</h4>
    <%= f.text_field :title %>

    <h4>本文</h4>
    <%= f.text_area :body %>

    <%= f.submit '投稿' %>

    <h4>画像</h4>
    <%= f.attachment_field :image %>

    <%= f.submit '投稿' %>

<% end %>